import 'package:flutter/material.dart';

class RecordReflejosTab extends StatelessWidget {
  const RecordReflejosTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
